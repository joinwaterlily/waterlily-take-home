const App = () => {
  return (
    <div className="min-h-screen flex items-center justify-center">
      <h1 className="text-4xl font-bold text-blue-500">Waterlily Take Home</h1>
      {/* TODO Step 1 - Create a table displaying all of the different funding sources in the funding-sources db"" */}
      {/* TODO Step 2. Time permitting, add the ability to add, edit, delete funding sources from the database. Exclude the id field. */}
    </div>
  );
};

export default App;
