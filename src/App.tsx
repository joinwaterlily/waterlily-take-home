const App = () => {
  return (
    <div className="min-h-screen flex items-center justify-center">
      <h1 className="text-4xl font-bold text-blue-500">Waterlily Take Home</h1>
      {/* TODO Step 1 - Create a table displaying all of the rows in the FundingSources table. Exclude the id field."" */}
      {/* TODO Step 2. Time permitting, add the ability to add, edit, delete funding sources from the database.  */}
    </div>
  );
};

export default App;
